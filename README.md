# bootwrapper.net
use macos keychain keys as temporary environment variables in c# projects (for local development).

> **warning:** macos keychain will hex code your key if it has any non-ascii characters (spaces, newlines, etc).
e
